#include<stdio.h>
#include<conio.h>
int main()
{
int inttype;
float floattype;
char chartype;
printf("%zu=is the type",sizeof(inttype));
printf("%zu is the tupe",sizeof(floattype));
printf("%zu is the type:",sizeof(chartype));
getch();
return 0;
}